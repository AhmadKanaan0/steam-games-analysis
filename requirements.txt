# scikit-learn is pinned exactly: the model bundle in models/ was pickled under
# 1.9.0, and unpickling into a different version can warn or fail outright.
scikit-learn==1.9.0

pandas==3.0.5
numpy==2.5.2
pyarrow==25.0.1
streamlit==1.63.0
altair==6.2.2
joblib==1.6.0
